# learn-react

```shell
# Install yarn, if you dont have it already installed on your machine. This is how you do it on mac
brew install yarn

# Install packages needed for this app (and listed in package.json)
yarn install

# Start the development server
yarn start
```
